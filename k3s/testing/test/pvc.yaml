apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: volv-pvc
  namespace: pv-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi