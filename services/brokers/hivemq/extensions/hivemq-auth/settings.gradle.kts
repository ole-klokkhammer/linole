rootProject.name = "hivemq-auth"

pluginManagement {
    plugins {
        id("com.hivemq.extension") version "2.1.0"
        id("de.fayard.refreshVersions") version "0.40.2"
    }
}

plugins {
    id("de.fayard.refreshVersions")
}